import Homepanel from './../../../src/components/Home/Homepanel';

describe('<Homepanel /> component', () => {
    it('should display correctly', () => {
        const enzymeWrapper = shallow(<Homepanel />);
    })
});